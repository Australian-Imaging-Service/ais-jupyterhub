apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cvmfs
  namespace: jupyter
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 1Gi  # Dummy value - actual storage from CVMFS
  storageClassName: cvmfs